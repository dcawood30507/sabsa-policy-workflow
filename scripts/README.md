# Utility Scripts

This directory contains utility scripts for managing and maintaining the SABSA Policy Workflow system.

## Future Scripts

Planned utilities:

- **`validate-policy.sh`** — Validate policy artifacts against JSON schemas
- **`generate-traceability-report.sh`** — Generate complete traceability matrix
- **`export-policy.sh`** — Export complete policy as single document
- **`cleanup-stale-branches.sh`** — Clean up abandoned PR branches
- **`migrate-config.sh`** — Migrate configuration format between versions

## Usage

Scripts will be documented here as they are developed.
